# Build Configuration for tunnel-gateway
VERSION="1"
BASE_IMAGE="images:alpine/3.20"
CONTAINER_NAME_PREFIX="tunnel-gateway"
STORAGE_PROVIDER="btrfs"
BUILD_CONTAINER_PREFIX="tunnel-gateway-build"

# Container will be minimal - no quota needed
# Packages: openssh-client, autossh, sshpass
