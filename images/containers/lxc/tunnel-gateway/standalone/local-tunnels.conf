# Tunnel Gateway - Standalone Configuration
# No container needed - tunnels run directly on your local machine
# All services accessible at 127.0.0.1:<local-port>

[mysql-link3]
# SSH Connection (to reach MySQL server)
sshAddress = 123.200.0.51
sshUsername = tbsms
sshPassword = TB@l38800
sshPort = 8210

# Tunnel Configuration
localPort = 13306
remoteHost = localhost
remotePort = 3306

# Database Credentials (for your application)
dbUsername = tbuser
dbPassword = Takay1#$ane
dbName = link3_sms

# Your app connects to:
#   JDBC: jdbc:mysql://127.0.0.1:13306/link3_sms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#   Username: tbuser
#   Password: Takay1#$ane


[kafka-local]
# SSH Connection (to reach Kafka container via host)
sshAddress = 123.200.0.50
sshUsername = tbsms
sshPassword = TB@l38800
sshPort = 8210

# Tunnel Configuration
localPort = 19092
remoteHost = localhost
remotePort = 29092

# Kafka Configuration (for your application)
kafkaBootstrapServers = 127.0.0.1:19092

# Your app connects to:
#   bootstrap.servers=127.0.0.1:19092
