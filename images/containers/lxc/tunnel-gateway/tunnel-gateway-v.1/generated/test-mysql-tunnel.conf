# Tunnel Gateway Configuration - MySQL Test
# Test SSH tunneling to local MySQL container

# Container Configuration
CONTAINER_NAME="tunnel-gateway-test"
CONTAINER_IP="10.10.199.150"  # Static IP from LXC private range

# SSH Tunnel Definitions
# Format: NAME:LOCAL_PORT:SSH_HOST:SSH_USER:AUTH_TYPE:AUTH_VALUE:REMOTE_HOST:REMOTE_PORT
TUNNELS=(
    # MySQL container via SSH tunnel
    "mysql-tunnel:3306:10.10.199.171:mustafa:PASSWORD:a:127.0.0.1:3306"
)

# Optional: Bind mounts for SSH keys
BIND_MOUNTS=()

# Auto-start tunnels on container boot
AUTO_START="true"
