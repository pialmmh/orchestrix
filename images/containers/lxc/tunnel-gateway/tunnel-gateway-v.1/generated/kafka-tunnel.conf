# Tunnel Gateway Configuration - Kafka Test
# Test SSH tunneling to local Kafka broker

# Container Configuration
CONTAINER_NAME="tunnel-gateway-kafka"
CONTAINER_IP="10.10.199.150"  # Static IP from LXC private range

# SSH Tunnel Definitions
# Format: NAME:LOCAL_PORT:SSH_HOST:SSH_USER:AUTH_TYPE:AUTH_VALUE:REMOTE_HOST:REMOTE_PORT
TUNNELS=(
    # Kafka broker via SSH tunnel
    "kafka-tunnel:9092:10.10.199.200:mustafa:PASSWORD:a:10.10.199.200:9092"
)

# Optional: Bind mounts for SSH keys
BIND_MOUNTS=()

# Auto-start tunnels on container boot
AUTO_START="true"
