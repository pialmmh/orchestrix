package com.orchestrix.stellar.model;

public enum Kind { 
    // Original entities (kept for backward compatibility)
    PERSON, ORDER, INVOICE,
    
    // Northwind entities
    CATEGORY, PRODUCT, CUSTOMER, SALESORDER, ORDERDETAIL, 
    EMPLOYEE, SHIPPER, SUPPLIER
}
