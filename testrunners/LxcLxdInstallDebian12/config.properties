# LXC/LXD Installation Test Configuration for Debian 12
# =====================================================

# SSH Connection Settings
ssh.host=103.95.96.76
ssh.port=22
ssh.username=telcobright
ssh.password=Takay1#$ane%%

# Installation Options
install.use.sudo=true
install.lxc=true
install.lxd=true
install.use.snap=false  # Use APT instead of Snap for LXD

# Test Scenarios to Run
test.status=true        # Check current installation status
test.install=false      # Perform installation (disabled for now - check status first)
test.verify=false       # Verify installation
test.bridge.config=false  # Configure bridge networking (uses LxdBridgeNetworkingAutomation)

# Logging Level (DEBUG, INFO, WARN, ERROR)
logging.level=INFO