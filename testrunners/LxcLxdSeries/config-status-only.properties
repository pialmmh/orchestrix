# LXC/LXD Status Check Configuration
# ===================================
# Only checks installation status, doesn't install anything

# Terminal Type
terminal.type=SSH

# Connection Settings
ssh.host=103.95.96.76
ssh.port=22
ssh.username=telcobright
ssh.password=Takay1#$ane%%

# Series Execution Settings
series.stop.on.failure=false

# Only run status checks (no installation)
# Will check status and skip if already installed
run.install=true
# Will check bridge and skip if configured
run.bridge.config=true

# Installation Runner Configuration
install.use.sudo=true
install.lxc=true
install.lxd=true
install.use.snap=false
# Don't force - will skip if already installed
install.force=false

# Bridge Networking Runner Configuration
bridge.name=lxdbr0
bridge.gateway.ip=10.0.8.1
bridge.network.cidr=10.0.8.0/24
bridge.enable.nat=true
bridge.enable.dns=true
bridge.dhcp.start=10.0.8.2
bridge.dhcp.end=10.0.8.254
bridge.delete.existing=false
# Don't force - will skip if already configured
bridge.force=false

# Logging Level
logging.level=INFO